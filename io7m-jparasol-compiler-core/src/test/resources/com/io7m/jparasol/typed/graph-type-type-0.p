package x.y;

module M is
  type t is record
    x : integer
  end;
  type u is record
    x : integer
  end;
  type v is record
    x : integer
  end;
end;

module N is
  type t is record
    x : integer
  end;
  type u is record
    x : integer
  end;
  type v is record
    x : integer
  end;
end;

module P is
  type t is record
    x : integer
  end;
  type u is record
    x : integer
  end;
  type v is record
    x : integer
  end;
end;
